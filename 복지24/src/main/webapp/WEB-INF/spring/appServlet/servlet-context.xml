<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the webapp root directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>

	<!-- 관리자 권한 검증 Interceptor -->
	<beans:bean id="adminInterceptor" class="com.greenart.bdproject.interceptor.AdminInterceptor" />

	<!-- 자동 로그인 Interceptor -->
	
	<beans:bean id="autoLoginInterceptor" class="com.greenart.bdproject.interceptor.AutoLoginInterceptor">
		<beans:property name="memberDao" ref="projectMemberDaoImpl" />
		<beans:property name="dataSource" ref="dataSource" />
	</beans:bean>

	<interceptors>
		<!-- 자동 로그인 인터셉터 -->
		<interceptor>
			<mapping path="/**"/>
			<exclude-mapping path="/resources/**"/>
			<exclude-mapping path="/api/auth/login"/>
			<exclude-mapping path="/api/auth/logout"/>
			<exclude-mapping path="/api/auth/register"/>
			<beans:ref bean="autoLoginInterceptor"/>
		</interceptor>

		<!-- 관리자 권한 인터셉터 -->
		<interceptor>
			<mapping path="/api/admin/**"/>
			<beans:ref bean="adminInterceptor"/>
		</interceptor>
	</interceptors>

	<context:component-scan base-package="com.greenart.bdproject" />
</beans:beans>